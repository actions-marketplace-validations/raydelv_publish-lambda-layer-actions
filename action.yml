name: 'Publish AWS Lambda Layer w/Version'
description: 'Run PublishLayerVersion'
author: 'taotao2345'
inputs: 
  layer_name:
    description: 'Target layer name'
    required: true
  zip_file:
    description: 'Package zip file path'
    required: true
  runtime:
    description: 'Runtime you want your layer to be compatible with'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'
branding:
  icon: 'tag'
  color: 'gray-dark'
